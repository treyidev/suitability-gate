# Frontend environment (Vite). Copy to `.env.local` to override; all vars must be VITE_-prefixed to
# be exposed to the browser bundle.

# Base URL of the Java gateway. Defaults to the local dev backend when unset (see src/api/client.ts).
VITE_API_BASE_URL=http://localhost:8080
